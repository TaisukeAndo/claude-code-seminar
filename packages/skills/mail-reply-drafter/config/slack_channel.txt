# mail-reply-drafter の実行結果通知先Slackチャンネル
#
# 1行目（#で始まらない最初の行）に channel_id を記載する。
# channel_id はSlackの「チャンネル詳細」>「その他」からコピーできる値、
# または該当するSlack連携ツールでチャンネルを検索して得られるIDを使う。
#
# 初期状態はコメントのみ（未設定）。この場合SKILL.mdのStep 7に従い、
# Slackへの通知はスキップされる（Slack通知は任意機能のため）。
# 使いたい場合は、下の行に実際のchannel_idを書き足す。
#
# 記入例（先頭の # を外して、実際のIDに置き換える）:
# C0123456789
